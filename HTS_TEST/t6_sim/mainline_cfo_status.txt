Phase 3-alpha v2 — 메인트리(HTS_LIM/HTS_LIM) CFO 연결 조사 (읽기 전용)
================================================================================

(1) HTS_V400_Dispatcher_Core.cpp
    - cfo_.Init() in full_reset_() only (약 line 344).
    - 기타 cfo_ / Estimate_From_Preamble / cfo_.Apply 호출 없음.

(2) HTS_V400_Dispatcher_Sync.cpp
    - 주석: "CFO 는 Walsh 도메인에서 처리 예정 — MCE 철거 (Stage 1)" (P0 뒤),
            "CFO 는 Walsh 도메인에서 처리 예정 (시간도메인 Apply 철거, Stage 1)" (Feed_Chip).
    - Estimate_From_Preamble / cfo_.Apply: 소스에 없음.

(3) HTS_V400_Dispatcher_Payload.cpp / Decode.cpp
    - CFO 보상기 호출 없음 (grep cfo_ / Estimate_From_Preamble).

(4) HTS_V400_Dispatcher.hpp
    - 멤버 HTS_CFO_Compensator cfo_ 선언.
    - Stage 6A: "CFO-Walsh row permutation" 주석(행 치환) — 별도 시간도메인 Apply 아님.

(5) Walsh Bank / FWHT
    - HTS_PHASE0_WALSH_BANK 등은 프리앰블 스캔/지문 경로; CFO 추정기(MCE) 복구 없음.

결론 체크박스 (메인트리 기준)
  [x] 메인트리에서 CFO 시간도메인 Apply 완전 부재 (Init만, 경로 철거 상태)
  [ ] Walsh 도메인 CFO 전용 보상기로서 완결된 별도 블록은 미확인(주석만 존재)
  [ ] 부분 구현: CFO_Compensator 클래스는 HTS_CFO_Compensator.h에 구현되어 있으나
      메인트리 Dispatcher에 미연결.

양산 제품 CFO 대응: 현 메인트리는 CFO_Compensator 미배선 → 실측/양산 동작은
  "Walsh 도메인에서 처리 예정" 설계 주석과 불일치 가능. 시험은 t6_sim Local 에서만 Option 2 재배선.

(추가) HTS_CFO_Compensator.h 에 Advance_Phase_Only(n) 추가:
  Estimate 직후 P0 192칩에 해당하는 위상 누적 선행(Apply 와 동일 스텝).

하네스 Option 3: HTS_PHASE3_ALPHA_TX_CFO (기본 0).
  전체 패킷 TX 위상 회전 + RX Estimate/Apply/Advance 조합은 현재 체인에서
  동기 실패(0% 수렴) — v2 기본 빌드는 TX CFO 비활성, RX CFO 재배선만 활성.
  TX 실측은 /DHTS_PHASE3_ALPHA_TX_CFO=1 별도 빌드 후 튜닝 필요.
